h4 Оставить отзыв
= simple_form_for([@user, @user.reviews.build]) do |f|
  #rating-form
    label Rating
  = f.input :comment
  = f.button :submit, class: "btn btn-compose"

javascript:
  $('#rating-form').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });
